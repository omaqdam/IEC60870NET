<Window x:Class="IEC60870.TestHarness.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IEC60870.TestHarness.Views"
        mc:Ignorable="d"
        Title="IEC 60870-5-104 Test Harness"
        Height="450" Width="900">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBox Width="150" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="80" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="80" Text="{Binding CommonAddress, UpdateSourceTrigger=PropertyChanged}" />
            <Button Width="110" Content="Connect" Command="{Binding ConnectCommand}" />
            <Button Width="110" Content="Disconnect" Command="{Binding DisconnectCommand}" />
            <Button Width="150" Content="GI (C_IC_NA_1)" Command="{Binding GeneralInterrogationCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Events}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,12,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="200" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150" />
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding Status}" FontWeight="SemiBold" />
    </Grid>
</Window>
